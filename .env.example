# Read by docker-compose-dev.yaml only. Copy to .env, which git ignores.
# Use the same value as DB_PASSWORD in project/node/src/.env. It only takes
# effect when the db volume is first created.
MARIADB_ROOT_PASSWORD=
